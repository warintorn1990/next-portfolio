export default ({ children, title }) => (
    <div>
        <header>header</header>
        <h1>{title}</h1>
        {children}
        <footer>footer</footer>
    </div>
)

{/* <Layout>
    <h1>Home</h1>
</Layout> */}